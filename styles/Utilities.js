import { StyleSheet } from "react-native";

const Utilities  = StyleSheet.create({
    marginBottom: {
        marginBottom: 10
    },
    marginBottomMedium: {
        marginBottom: 20
    },
    marginBottomBig: {
        marginBottom: 30
    }
})

export default Utilities;